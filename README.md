# LAKEX

Premium technology studio portfolio built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

The hero image is an external Unsplash image defined in `src/styles.css`, so it can be replaced easily. The contact form is frontend-only and displays a success state on submit.
